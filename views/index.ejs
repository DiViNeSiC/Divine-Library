<h2>Recently Added</h2>
<div>
    <% books.forEach(book => { %>
        <div><%= book.title %></div>
        <div>
            <img src="<%= book.coverImagePath %>" height="100px" width="100px" alt="err">
        </div>
    <% }) %>
</div>